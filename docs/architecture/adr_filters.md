| Item | Type | Options | Behaviour | My library | Explore | Favourites | Workflows |
|---|---|---|---|---|---|---|---|
| Sort | Single select | - A–Z<br>- Date created | - Sort control on cards.<br>- Canonical placeholder label is `Sort`.<br>- On first load, the list of cards is ordered by most recently modified items.<br>- First click on an option applies ascending order; second click on the same option toggles to descending.<br>- When a non-default sort is active, the control shows that option’s name as the visible label instead of `Sort`, together with a reset button `(X)`. | X | X | X | X |
| Discipline | Multiselect | All disciplines which are available in the `Create project` form. | - Multi-select discipline type filter on cards.<br>- Label is `Discipline`.<br>- By default, the list of cards includes all disciplines, same list as Disciplines field on `Create project` form.<br>- When there are no results for an option in the query context, that option’s checkbox is read-only and cannot be clicked.<br>- When the user has selected at least one option, the filter shows a number indicator on the top right, showing the number of options selected.<br>- User clears the filter by manually unselecting selected options or using the `None` control; user may select all options via the `All` control. |  | X |  |  |
| Ownership | Single select | - Owned<br>- Shared | - Single-select ownership filter on cards.<br>- Canonical placeholder label is `Ownership`.<br>- On first load, the list of cards includes both options.<br>- When an option is active, the visible label is that option name instead of `Ownership`, together with reset button `(X)`.<br>- Clicking the reset button clears the active ownership selection and restores placeholder `Ownership`. | X |  | X | X |
| Type | Single select | - Project<br>- Workflow | - Single-select type filter on cards.<br>- Label is `Type`.<br>- On first load, the list of cards includes both types.<br>- When an option is active, the visible label is that option name instead of `Type`, together with reset button `(X)`.<br>- Clicking the reset button clears the active type selection and restores placeholder `Type`. | X | X | X |  |
| Workflow type | Multiselect | - Activity<br>- Course<br>- Program | - This filter displays only with `Type` includes `Workflow`; the only exception is the `Workflows` view since the view is only showing Workflows.<br>- Multi-select workflow type filter on cards.<br>- When there are no results for an option in the query context, that option’s checkbox is read-only and cannot be clicked.<br>- Canonical placeholder label is `Workflow type`.<br>- By default, the list of cards includes all workflow types.<br>- When the user has selected at least one option, the filter shows a number indicator on the top right, showing the number of options selected.<br>- User clears the filter by manually unselecting selected options or using the `None` control; user may select all options via the `All` control. | X | X | X | X |
| Favourites | Boolean |  | - Boolean filter.<br>- When on, results are limited to the user’s favourites.<br>- When off, favourites filter is not applied; favourites may still appear among other results. | X | X |  | X |
| Templates | Boolean |  | - Boolean filter.<br>- When on, results are limited to the `Template` workflows.<br>- When off, filter is not applied; templates may still appear among other results. |  | X | X |  |
| Archives | Boolean |  | - Boolean filter.<br>- When on, results are limited to archived workflows and/or projects.<br>- When off, filter is not applied; archived workflows and projects `MAY NOT` still appear among other results. | X |  |  | X |
