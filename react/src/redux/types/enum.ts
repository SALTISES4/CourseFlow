// Slice namespaces

// @todo out of date
export enum SliceNamespace {
  NODE = 'node',
  SIDEBAR = 'sidebar',
  SVGLINK = 'svglink',
  SECTION = 'section',
  COLUMN = 'column',
  STRATEGY = 'strategy',
  NODELINK = 'nodelink',
  WORKFLOW = 'workflow',
  PROJECT = 'project',
  VIEWSETTINGS = 'viewsettings'
}
